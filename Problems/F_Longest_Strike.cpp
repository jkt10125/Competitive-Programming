#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while(t--) {
        int n, k;
        cin >> n >> k;
        map<int, int> A;
        for(int i=0; i<n; i++) {
            int x;
            cin >> x;
            A[x]++;
        }

        int l, r, ctr=0;
        for(auto it : A) {
            if(it.second )
        }
    }
}