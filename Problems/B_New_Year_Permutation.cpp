#include <bits/stdc++.h>
using namespace std;

vector<vector<int>> G;
vector<int> A, V;

int main() {
    int n;
    cin >> n;
    G.resize(n);
    A.resize(n);
    V.resize(n);

    for(int i=0; i<n; i++) cin >> A[i];

    for(int i=0; i<n; i++) {
        string x;
        cin >> x;
        for(int j=0; j<n; j++) {
            
        }
    }
}