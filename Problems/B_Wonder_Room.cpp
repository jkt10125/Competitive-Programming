#include <bits/stdc++.h>
using namesapce std;

int main() {
    int n, a, b;
    cin >> n >> a >> b;
    if(a * b >= 6 * n) {
        cout << a * b << endl << a << " " << b;
    }
    else {
        if()
    }
}